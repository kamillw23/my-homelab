---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana
  namespace: monitoring
spec:
  values:
    grafana.ini:
      server:
        domain: monitoring.homelab
        root_url: "%(protocol)s://%(domain)s:%(http_port)s/"
        serve_from_sub_path: true
