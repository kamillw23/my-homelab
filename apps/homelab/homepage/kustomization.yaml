apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/homepage
patches:
  # Change host name and path in Ingress resource
  - target:
      kind: Ingress
      name: homepage
    patch: |
      - op: replace
        path: "/spec/rules/0/host"
        value: "homelab.local"
      - op: replace
        path: "/spec/rules/0/http/paths/0/path"
        value: "/"

  # Change allowed hosts for homepage deployment
  - target:
      kind: Deployment
      name: homepage
    patch: |
      - op: replace
        path: "/spec/template/spec/containers/0/env/0/value"
        value: "homelab.local"

  # Add entries to settings.yaml file for homepage
  - patch: |
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: homepage
        namespace: homepage
      data:
        settings.yaml: |
          title: My custom homelab Homepage
          theme: dark
          headerStyle: clean
